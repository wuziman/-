name: Daily Stock Analysis

on:
  schedule:
    # 北京时间早上9点 = UTC时间凌晨1点（提前5分钟确保准时）
    - cron: '55 0 * * *'
  workflow_dispatch:  # 允许手动触发

jobs:
  analyze:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: '3.9'

      - name: Install dependencies
        run: |
          pip install pandas numpy requests openpyxl yfinance fredapi newsapi-python beautifulsoup4 lxml

      - name: Update stock data
        run: python fetch_my_stocks.py

      - name: Run analysis
        run: python daily_stock_report.py
        env:
          # 企业微信Webhook URL（在GitHub仓库Settings → Secrets中配置）
          WECHAT_WEBHOOK: ${{ secrets.WECHAT_WEBHOOK }}
          # API Keys（在GitHub仓库Settings → Secrets中配置）
          NEWSAPI_KEY: ${{ secrets.NEWSAPI_KEY }}
          FRED_API_KEY: ${{ secrets.FRED_API_KEY }}
