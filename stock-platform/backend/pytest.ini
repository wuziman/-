[pytest]
# Collect tests only from tests/ (root-level test_*_manual.py are manual scripts, not auto-discovered).
# Keep this file ASCII-only: iniconfig reads it with the system default encoding.
testpaths = tests
