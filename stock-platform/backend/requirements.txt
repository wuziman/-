# Web框架（版本钉死为本机验证通过的组合，升级前先跑 pytest）
fastapi==0.136.1
uvicorn==0.47.0

# 数据库
sqlalchemy==2.0.25

# 股票数据（A股走新浪直连requests，美股走yfinance）
yfinance==1.3.0

# 数据处理
pandas==2.1.4
numpy==1.26.4

# HTTP请求
requests==2.31.0

# 数据验证
pydantic==2.13.4

# 定时任务调度（4.x移除pytz，钉在3.x）
apscheduler==3.11.3
pytz==2023.3.post1

# 测试/Lint（此前pytest混在生产依赖，暂保留；ruff仅本地/CI用）
pytest==7.4.0
ruff==0.16.4
