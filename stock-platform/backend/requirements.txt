# Pin to the exact combination verified on the local machine (py3.11).
# Upgrade deliberately: bump a pin, then run pytest before committing.
# NOTE: keep this file ASCII-only -- pip decodes it with the system locale
# (GBK on zh-CN Windows), so non-ASCII comments crash `pip install`.

# Web framework
fastapi==0.136.1
uvicorn==0.47.0

# Database
sqlalchemy==2.0.25

# Stock data (US via yfinance; A-share via requests direct)
yfinance==1.3.0

# Data processing
pandas==2.1.4
numpy==1.26.4

# HTTP client
requests==2.31.0

# Validation
pydantic==2.13.4

# Scheduler (pin 3.x: 4.x drops pytz)
apscheduler==3.11.3
pytz==2023.3.post1

# Test / lint (pytest kept here for CI simplicity; ruff for local/CI)
pytest==7.4.0
ruff==0.16.4
